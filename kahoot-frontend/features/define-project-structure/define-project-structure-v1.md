## Goal
define project structure

## Description
I am thinking of defining project structure before it get bigger and missy , such tat waanna sparate component and page in correct way where this will help locate where this,
page or component being refrence.

## Tasks
- [ ] move files and folder such as secnior angular developer.
- [ ] move components will always as two files html and ts so we always will used ng g component.
- [ ] component structure will be

-> src/
    -> /core/ keeps it same
    -> /pages/ ## pages  can have sub folder pages
        /pages/quizeportal/ ## quizeportal will be feature module for example 
        /pages/quizeportal/create/quizecreate.page.componentn.ts
        /pages/quizeportal/preview/quizepreview.page.componentn.ts
        /pages/quizeportal/component/create/questioneditor.quizecreate.componentn.ts
        /pages/quizeportal/component/create/questinsidebar.quizecreate.componentn.ts
        /pages/quizeportal/component/preview/questionpreview.quizeprview.componentn.ts
        /pages/quizeportal/component/shared/component/questiontype/questiontitle.component.ts


## Rules 
1. every page should have naming of pagename.page.componentn.ts all small letter no special character allow like - 
2. componnent name it self in file should be camel case so component name TrueFalse but file name would be truefalse.component.ts
3. all componentn , pages must have two files html , ts.
3. all pages should be renamed to follow standard like sigin.page.componentn.ts but class name should be SiginComponent.
4. please for quize portal it will include every thing related to quize like create , preview and its sub component but not quizes.
5. for selector of components we should follow [page-*component] and if its sub component will be like [sub-*componentName]


## Instruction
1. I know its very huge refcator so wil take it step by step. so u should provide ur plan and show it to me before implemention.
2. Don't guess if u have question please ask.
## Questions

Q1. `pages/quiz/quiz-layout.ts` is the router shell that wraps quiz-create and quiz-preview with a `<router-outlet>`. Does it move into `quizeportal/` (e.g. `quizeportal/quizeportal.layout.component.ts`) or stay outside in `pages/`?

Q2. `pages/quizzes/quizzes.ts` — rule 4 says quizzes stays outside quizeportal. New name: `pages/quizes/quizes.page.component.ts`? (one `z` or two in the folder/file name?)

Q3. TrueFalseOptions sub-component lives under TrueFalseComponent. In the new structure, where do question-type-specific sub-components go? e.g. `pages/quizeportal/component/shared/component/questiontype/truefalse/components/`?

Q4. Class names for pages — do the class names stay as-is (e.g. `QuizCreateComponent`, `SignInComponent`)? Only the file naming changes?

Q5. Selector pattern — are these element selectors or attribute selectors?
  - Element: `page-quizecreate` → used as `<page-quizecreate />`
  - Attribute: `[page-quizecreate]` → used as `<div page-quizecreate>`

Q6. `core/config/component.config.ts` — the lazy import path for TrueFalseComponent will change when it moves. Does `core/config/` stay where it is, or does it also relocate?

## Answers

Q1 . do really need it if so keep in same folder otherwise remove it please correct me if Iam wong.
Q2. quizzes to disply the quize so is more like listing so rename to be quizesplan.page.ts which more for reading.
Q3. yes sense option only used for truefalse we can make componentOption.truefalseComponentName.component.ts so we now this is specfic for true false 
Q4 for pages we should *PageName*PageComponent and for component will have *ComponentName*Component and do it for all files.
Q5. yes for pages for component and I changed my mind for sub component 
if component globally use "app" post fix 
if for feature like quize portal quizeportal-componentname so featurename-componentname
Q6 Don't change it.

## Instruction
1. please create skill for generating component with ng g c clik command and  skills for folder structure components naming as we discussed here, 


## Feedback
1. files withing sub folder such that such as component/create should be alsog in such folder once u created it with name Editorcanvas for example it will be in sub folder generated by angular when u do ng g path/componentName

2. so crete will have two subfolders , similary for preview and toher shared/component/questiontype/componentFolder/ -> html and ts.

3. change component to components.
